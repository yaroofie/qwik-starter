import { component$, HTMLAttributes } from '@builder.io/qwik';

export interface ThreeRowThreeColumnHeaderProps extends HTMLAttributes<HTMLDivElement> {
  // count
}

export const ThreeRowThreeColumnHeader = component$<ThreeRowThreeColumnHeaderProps>((props) => {
  return <header></header>
});