import { component$, HTMLAttributes } from '@builder.io/qwik';

export interface OneRowTwoColumnHeaderProps extends HTMLAttributes<HTMLDivElement> {
  // count
}

export const OneRowTwoColumnHeader = component$<OneRowTwoColumnHeaderProps>((props) => {
  return (
    <header>
      
    </header>
  );
});