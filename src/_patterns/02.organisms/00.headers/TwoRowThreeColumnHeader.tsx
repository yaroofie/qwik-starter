import { component$, HTMLAttributes } from '@builder.io/qwik';

export interface TwoRowThreeColumnHeaderProps extends HTMLAttributes<HTMLDivElement> {
  // count
}

export const TwoRowThreeColumnHeader = component$<TwoRowThreeColumnHeaderProps>((props) => {
  return <header></header>
});