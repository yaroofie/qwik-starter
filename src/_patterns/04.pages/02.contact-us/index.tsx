import {component$} from '@builder.io/qwik'

export interface ContactUsPageProps {
  // count
}

export const ContactUsPage = component$<ContactUsPageProps>((props) => {
  return <div>Hello Qwik!</div>
})
