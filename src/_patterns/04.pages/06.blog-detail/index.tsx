import {component$} from '@builder.io/qwik'

export interface BlogDetailPageProps {
  // count
}

export const BlogDetailPage = component$<BlogDetailPageProps>((props) => {
  return <div>Hello Qwik!</div>
})
