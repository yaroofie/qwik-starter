import {component$} from '@builder.io/qwik'

export interface ProductsPageProps {
  // count
}

export const ProductsPage = component$<ProductsPageProps>((props) => {
  return <div>Hello Qwik!</div>
})
