import { component$ } from '@builder.io/qwik';

export interface AboutUsPageProps {
  // count
}

export const AboutUsPage = component$<AboutUsPageProps>((props) => {
  return <div>Hello Qwik!</div>
});