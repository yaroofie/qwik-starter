import {component$} from '@builder.io/qwik'

export interface UserProfilePageProps {
  // count
}

export const UserProfilePage = component$<UserProfilePageProps>((props) => {
  return <div>Hello Qwik!</div>
})
